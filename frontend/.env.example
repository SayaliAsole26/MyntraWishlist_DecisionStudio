# Local dev: leave empty — Vite proxies /api to http://127.0.0.1:8002
# Production (Vercel): set to your Railway backend URL (no trailing slash)
# VITE_API_URL=https://your-api.up.railway.app
VITE_API_URL=
